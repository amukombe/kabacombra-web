<p style="color: green"><%= notice %></p>

<%= render @beer_dispatch %>

<div>
  <%= link_to "Edit this dispatch", edit_beer_dispatch_path(@beer_dispatch) %> |
  <%= link_to "Back to dispatches", beer_dispatches_path %>

  <%= button_to "Destroy this dispatch", @beer_dispatch, method: :delete %>
</div>
