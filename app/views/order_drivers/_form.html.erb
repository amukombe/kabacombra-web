<%= form_with(model: order_driver) do |form| %>
  <% if order_driver.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order_driver.errors.count, "error") %> prohibited this order_driver from being saved:</h2>

      <ul>
        <% order_driver.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :order_id, style: "display: block" %>
    <%= form.text_field :order_id %>
  </div>

  <div>
    <%= form.label :driver_id, style: "display: block" %>
    <%= form.text_field :driver_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
